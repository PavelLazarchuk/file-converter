// `<ViewTransition>` ships in the React build Next bundles, but @types/react keeps
// it behind the canary entry point. Pulling that in makes it typed here too.
/// <reference types="react/canary" />
